
import ContactClient from "./ContactClient";

export const metadata = {
    title: "Mode to Code | Contact",
    description: "Get in touch with Mode to Code. We would love to hear from you!",
}

const page = () => {
    return (
        <ContactClient />
    )
}

export default page