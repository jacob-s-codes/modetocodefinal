import LearnClient from './LearnClient'

export const metadata = {
    title: "Mode to Code | Learn",
    description: "Use these free resources to start learning AI, Web Development, and Game Development.",
}

const page = () => {
    return (
        <div>
           <LearnClient />
        </div>

    )
}

export default page